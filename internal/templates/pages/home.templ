package pages

import (
	"github.com/tneuqole/habitmap/internal/session"
	"github.com/tneuqole/habitmap/internal/templates/layouts"
)

templ Home(data session.SessionData) {
	@layouts.Base(data, "Home") {
		<div class="flex flex-col items-center justify-center text-center py-8 px-4">
			<p class="mt-2 text-base text-gray-600">Welcome to Habitmap</p>
		</div>
	}
}
